<% @messages.each do |message| %>
  $('.chat-dialog').find('.chat-node').last().after(`<%= render 'chats/nodes/chat_node',
    message: message,
    direction: @direction,
    timestamp: @timestamp %>`);
<% end %>

initSuggestions();